{
  "name": "wallet-radar",
  "version": "0.1.0",
  "private": true,
  "description": "Hardware wallet price radar",
  "scripts": {
    "start": "node -r ./price-reader-fix.js -r ./trezor-api-fix.js -r ./amazon-price-fix.js -r ./price-history.js -r ./idealo-price-source.js -r ./trezor-official-price-source.js -r ./currency-runtime.js -r ./wallet-radar-layout.js -r ./display-fix.js -r ./server-live-patch.js server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
